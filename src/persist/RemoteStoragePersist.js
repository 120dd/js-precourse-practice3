import { DataPersist } from './DataPersist';

export class RemoteStoragePersist extends DataPersist {
  // eslint-disable-next-line no-unused-vars
  save(key, value) {
    // TODO
  }

  // eslint-disable-next-line no-unused-vars
  load(key) {
    // TODO
  }
}
